name:practice
  on:[push,workflow_dispatch]
  jobs:
    name:Test
      runs-on:ubuntu latest
      steps:
        -name:get code
          uses:actions/checkout@v3
          run:echo 'code checked out'
